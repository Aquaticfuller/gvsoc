from gvtest import *

# Called by gvtest to declare the tests
def testset_build(testset):

    testset.set_name('deprecated')

    # Both of these exercise targets that no longer exist: pulp.spatz.spatz and snitch_spatz
    # were removed from the pulp submodule upstream (65f04b3, 2026-06-11) and are no longer in
    # the default TARGETS, so neither test can pass. Re-enable them if the targets return.
    # testset.import_testset(file='spatz/testset.cfg')
    # testset.import_testset(file='snitch_spatz/testset.cfg')
